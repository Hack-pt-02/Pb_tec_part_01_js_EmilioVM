
// CÁLCULO DEL IVA

/* let datos = prompt("introduce un numero para calcular el IVA");

let calc = () => datos * 1.21;

document.write (`<h1> el dato introducido, que es ${datos}, sumandole el 21% es ${calc()}</h1>`) */

// DETERMINAR SI UN NÚMERO ES MÚLTIPLO


/* let otrosDatos = prompt("¿será multiplo el número introducido?")

function multi() {

    while (true) {
        if ((otrosDatos % 2) == 0) {
            alert (`sí que es multiplo de 2`)
            break
        } else {
            otrosDatos = prompt("ERROR, prueba de nuevo")
        }
    }

}

multi() */